#!/usr/bin/python3

import sys

def main():
    try:
        input_data = sys.stdin.read().split()

        if len(input_data) < 2:
            sys.exit(1)

        base = float(input_data[0])
        max_n = int(float(input_data[1]))

    except ValueError:
        sys.exit(1)

    for i in range(1, max_n + 1):
        val = base ** i
        print(val)

if __name__ == "__main__":
    main()
